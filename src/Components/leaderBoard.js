import React from 'react';
import './Styles.css'
const leaderBoard = () => {

    const bodyStyle = 'bodyStyle'
    const leaderBoard = 'leaderBoard'
    const combinedClasses = `${bodyStyle} ${leaderBoard}`;
    return (
        <div className={combinedClasses}>

        </div>
    );
};

export default leaderBoard;
